# Accessibility

- `prefers-reduced-motion` respected site-wide
- Focus-visible states on interactive controls
- Do not hide meaningful content with `aria-hidden` during reveals
- Magnetic / spotlight: desktop fine-pointer only
- Keyboard can reach all CTAs and nav
- Color contrast: dark text on light surfaces; primary buttons use on-primary
