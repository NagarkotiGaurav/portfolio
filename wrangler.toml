# Cloudflare Pages project (React dist + functions/)
# Deploy with: npx wrangler pages deploy dist
# Do NOT use `wrangler deploy` — that is for Workers and breaks Pages.
name = "portfolio"
compatibility_date = "2025-08-09"
pages_build_output_dir = "dist"
